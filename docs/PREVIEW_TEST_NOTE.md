# Temporary preview-deployment trigger

This file exists only to give this throwaway branch a diff against
`feature/landing-page-v2`, so GitHub will open a PR and Vercel builds a
Preview deployment for testing the Paddle sandbox webhook end-to-end
(see docs/06_DECISIONS.md ADR-066).

Safe to delete once the Paddle sandbox testing pass is done — this
branch and its PR are never meant to be merged.
